import { AlertDialog } from '@radix-ui/react-alert-dialog';

const Home = () => {
  return (
    <main>
      <AlertDialog open={true}> hello world</AlertDialog>
    </main>
  );
};

export default Home;
